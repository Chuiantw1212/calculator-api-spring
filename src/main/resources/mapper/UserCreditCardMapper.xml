<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.en_chu.calculator_api_spring.mapper.UserCreditCardMapper">

    <resultMap id="BaseResultMap" type="com.en_chu.calculator_api_spring.entity.UserCreditCard">
        <id column="id" property="id" jdbcType="BIGINT" />

        <result column="firebase_uid" property="firebaseUid" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        
        <result column="deduction_account" property="deductionAccount" jdbcType="VARCHAR" />
        <result column="usage_type" property="usageType" jdbcType="VARCHAR" />
        <result column="storage_location" property="storageLocation" jdbcType="VARCHAR" />

        <result column="average_monthly_expense" property="averageMonthlyExpense" jdbcType="NUMERIC" />

        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP" />
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="Base_Column_List">
        id, firebase_uid, name, deduction_account, usage_type,
        storage_location, average_monthly_expense,
        created_at, updated_at
    </sql>

    <select id="selectByUid" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user_credit_cards
        WHERE firebase_uid = #{firebaseUid}
        ORDER BY created_at DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM user_credit_cards
        WHERE id = #{id} AND firebase_uid = #{firebaseUid}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_credit_cards (
            firebase_uid,
            name,
            deduction_account,
            usage_type,
            storage_location,
            average_monthly_expense
        ) VALUES (
            #{firebaseUid},
            #{name},
            #{deductionAccount},
            #{usageType},
            #{storageLocation},
            #{averageMonthlyExpense}
        )
    </insert>

    <update id="update">
        UPDATE user_credit_cards
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="deductionAccount != null">deduction_account = #{deductionAccount},</if>
            <if test="usageType != null">usage_type = #{usageType},</if>
            <if test="storageLocation != null">storage_location = #{storageLocation},</if>
            <if test="averageMonthlyExpense != null">average_monthly_expense = #{averageMonthlyExpense},</if>

            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id} AND firebase_uid = #{firebaseUid}
    </update>

    <delete id="delete">
        DELETE FROM user_credit_cards
        WHERE id = #{id} AND firebase_uid = #{firebaseUid}
    </delete>

    <delete id="deleteByUid">
        DELETE FROM user_credit_cards
        WHERE firebase_uid = #{firebaseUid}
    </delete>

</mapper>